info:
  name: Revoke token
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/auth/revoke"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
