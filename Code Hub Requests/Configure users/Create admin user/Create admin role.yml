info:
  name: Create admin role
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/role/create"
  body:
    type: json
    data: |-
      {
        "roleName": "admin"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
