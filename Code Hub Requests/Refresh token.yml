info:
  name: Refresh token
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/auth/refresh-token"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
