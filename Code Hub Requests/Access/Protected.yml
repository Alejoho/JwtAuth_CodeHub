info:
  name: Protected
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/test/protected"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
