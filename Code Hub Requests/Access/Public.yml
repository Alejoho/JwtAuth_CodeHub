info:
  name: Public
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/test/public"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
